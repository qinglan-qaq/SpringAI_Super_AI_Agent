spring:
  ai:
    mcp:
      server:
        stdio: true               # 启用 stdio 传输
        name: qinglan-image-search
        version: 1.0.0
  main:
    web-application-type: none    # 禁用 Web 环境（stdio 模式不需要 Web）