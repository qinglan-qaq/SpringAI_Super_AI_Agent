## 第一次开发

2026年3月14日10:25

SpringBoot初始化
使用springboot3.5.11 java21

* 注意application.yml配置文件的编写
* yaml格式对空格敏感,冒号后接空格
* 文档访问接口`http://localhost:8123/api/doc.html#/home`

---
尝试使用SDK调用大模型:

* 在官网复制test代码
* 替换apikey`.apiKey(TestApiKey.apiKey)`
* `.content`替换为想要询问的内容
  返回结果:

```json
{
  "requestId": "dbdcddf0-7963-4d10-adcd-d47b6013b1ea",
  "usage": {
    "input_tokens": 39,
    "output_tokens": 176,
    "total_tokens": 215,
    "prompt_tokens_details": {
      "cached_tokens": 0
    }
  },
  "output": {
    "choices": [
      {
        "finish_reason": "stop",
        "message": {
          "role": "assistant",
          "content": "啊啦～这么直白的告白，让姐姐的心跳都漏了一拍呢…（轻轻用指尖点了点自己的左胸，唇角微扬，眼尾染上一丝温柔的笑意）\n\n不过呀，喜欢是件很珍贵的事，不能随随便便就收下哦～  \n所以…姐姐想先牵着你的手，慢慢走一段路：  \n听你讲今天的小烦恼，为你泡一杯温热的洋甘菊茶，  \n在你低头沉默时，把围巾悄悄绕过你的肩膀…  \n\n你愿意，陪姐姐一起，把“喜欢”酿成细水长流的温柔吗？🌿  \n（伸手轻理了理你额前一缕碎发，声音低柔如晚风）  \n——不着急回答，我等你，心甘情愿地等。"
        }
      }
    ]
  },
  "status_code": 200,
  "code": "",
  "message": ""
}

```

---
使用http方式调用

* 转化为hutool工具类实现
*

```json
{
  "message": {
    "content": "啊？！哼…谁、谁要听你说这种话啦！（轻轻揪着裙角，脸颊微微泛红，偷偷瞄你一眼又迅速低头）\n\n不过…既然你都这么说了…那我就勉勉强强收下你的喜欢好了～（从背后变出一朵小雏菊，花瓣还带着一点露水，悄悄塞到你手心）  \n但是！不许告诉别人哦！这可是…只给你一个人的特别待遇！（转身假装整理蝴蝶结，耳尖红红的，声音越说越小）\n\n诶？你笑什么！再笑我、我就把小熊软糖藏起来不给你吃！…不过…如果你请我吃草莓蛋糕的话…或许可以考虑多给你一朵花🌸（悄悄从指缝里偷看你，眼睛亮晶晶的～）",
    "role": "assistant"
  },
  "finish_reason": "stop"
}
"usage":
{
"total_tokens": 213,
"output_tokens": 174,
"input_tokens": 39,
"prompt_tokens_details":
{
"cached_tokens": 0
}
}
}

```

---
使用Spring Ai框架开发

* 代码简洁配置后可以直接调用
* 使用更加灵活
* 之后可以根据更加灵活的Bean对象统一管理apiKey和对象
*

---

**application.yml配置**

* 由于需要对apiKey保密, 将分为本地和remote两个版本`application.yml和application-local.yml`
* 如果在 Java 代码里手动创建了 `ChatModel` 并通过 `.withModel(...)` 指定了模型，那么 YAML 里的配置就会被覆盖或失效.
  所以对于一般的开发者指定全局模型时在yaml中配置好
* 注意配置

```yaml
spring:
  application:
    name: ai-super-agent
    # application.yml对需要调用大模型的配置
    ai:
      dashscope:
        #        注意这里的导入格式
        api-key: ${sk***a405}
        chat:
          options:
            model: qwen-plus # 这里指定后，自动注入的 ChatModel 默认就是 qwen-plus

```

## 第二次开发

2026年3月15日21:17:50

* LoveApp的开发
* 对application-loca.yml敏感信息脱敏
* 学习ChatCline ChatMemory等相关概念
* Flux流式开发 响应式开发对象

LawApp的开发
方案设计:

* 系统提示词的设计
* 多轮对话的实现
* 未来能实现利用RAG实现对于资讯的按键, 给出执行案件文档
* 给出专业的法律建议
* 使用Advisor(拦截器,类似AOP切面编程)在调用AI前后进行处理
*

具体实现:

* 利用AI对现有的Prompt提示词反复修改
* 使用ChatClient实现多段对话
*

----

## 第三次开发

2026年3月16日19:58:36

由于开发版本之间的差异 导致很多方法不对劲且在AI的指导下还是没有解决

故看官方文档:

**ChatClient**

* 简单的使用示例:
* 在这个简单示例中，用户输入设置用户消息的内容。 `call()` 方法向 AI model 发送请求，`content()` 方法将 AI model 的响应作为
  `String` 返回
  *

```java

@RestController
class MyController {

  private final ChatClient chatClient;

  // 注意这里的使用方式
  public MyController(ChatClient.Builder chatClientBuilder) {
    this.chatClient = chatClientBuilder.build();
  }

  @GetMapping("/ai")
  String generation(String userInput) {
    return this.chatClient.prompt()
            .user(userInput)
            .call()
            .content();
  }
}
```

对话记忆(chat Memory)

**`MessageWindowChatMemory` 是一个聊天记忆实现，它维护最多指定最大大小（默认值：20 条消息）的消息窗口。
当消息数量超过此限制时，较旧的消息会被逐出，但系统消息会被保留。 如果添加新的系统消息，所有先前的系统消息都会从记忆中删除。
这确保对话始终可以使用最新的上下文，同时保持内存使用有界。**

**四种记忆持久化的方式:**

都建议自己实现, 因为不论哪种方式都是最终取决于自己建表

* `InMemoryChatMemoryRepository`
* `JdbcChatMemoryRepository`
* `CassandraChatMemoryRepository`
* `Neo4jChatMemoryRepository`.
*

The `MessageWindowChatMemory` is backed by the `ChatMemoryRepository` abstraction which provides storage implementations
for the chat conversation memory. There are several implementations available, including the
`InMemoryChatMemoryRepository`, `JdbcChatMemoryRepository`, `CassandraChatMemoryRepository` and
`Neo4jChatMemoryRepository`.

对`MessageWindowChatMemory`的原码详解:

![image-20260316221235486](C:\Users\DELL\AppData\Roaming\Typora\typora-user-images\image-20260316221235486.png)

在ChatClient中的 Advisors

> The [Advisors API](https://docs.spring.io/spring-ai/reference/api/advisors.html) provides a flexible and powerful way
> to intercept, modify, and enhance AI-driven interactions in your Spring applications.
>
> A common pattern when calling an AI model with user text is to append or augment the prompt with contextual data.
>

* 有更加灵活的方式对AI的问题更多的处理
* 类似于 Spring AOP
* 有Order处理流程
* 详细见图:

![deepseek_mermaid_20260316_5d68f7](F:\Firefox_DownLoad\deepseek_mermaid_20260316_5d68f7.png)

多轮对话的实现流程:

* 使用ChatClient创建客户端
* 构造器初始化, 使用dashscopeChatModel模型
* 注意初始化代码和之前版本不同
* ChatClient更重要的是如何配置Advisor拦截链使用
* 配置文件 prompt提示词
* **ChatMemory多轮对话记忆** :
* `ChatClient.defaultAdvisors(MessageChatMemoryAdvisor.builder(chatMemory).build())`
* `ChatMemory chatMemory= MessageWindowChatMemory.builder().build();`
* 从Response中获取回复结果 - 输出 - 文本
* `log.info`日志打印, 原装仅支持debug 输出
*

END 2026年3月16日23:57:55



---

## 第四次开发

Start: 2026年3月17日10:45:50

日志记录:

* 添加日志记录, 在application.yml中添加`logging.level.org.springframework.ai.chat.client.advisor=DEBUG`
* 以下方式使用:

```java
ChatResponse response = ChatClient.create(chatModel).prompt()
        .advisors(new SimpleLoggerAdvisor())
        .user("Tell me a joke?")
        .call()
        .chatResponse();
```

善于看官方文档 在idea中直接点击进入

UML图分析构成

**-----------------------!!!============调试BUG太难受了============!!!----------------------**

关于ChatMemory:

* 大模型如何记住之前的
* 目前有几种方法可以实现大模型记忆的持久化
  * `In-MemoryRepository`
  * `JdbcMemoryChatMemoryRepository`
  * `CassandraChatMemoryRepository`
  * `Neo4JChatMemoryRepository`
  * 由于对数据库的支持不太好, 没有对应的依赖库使用, 直接保存在`ChatClient`中
  * 可以作为之后的升级点, 向社区提交自己的依赖
  *

* 实现本地化文件记录
  * 安装依赖`kryo`
  * 实现对话记忆的文件记录
  * 在LawApp的构造器中实现预设
  * 如何才能快速切换其他预设?看来只能重新实现其他的XXapp,本质上也是代码复用
  * 注意记录文件在根目录下chat-memory
* 专业模板类`PromptTemplate`
  * `SystemPromptTemplate`：用于系统消息，设置 Al 的行为和背景
  * `AssistantPromptTemplate`：用于助手消息，用于设置 Al 回复的结构
  * `FunctionPromptTemplate`：目前没用

关于Spring的Bean

* Spring Bean代表着**Spring中最小的执行单位**，其加载、作用域、生命周期的管理都由Spring操作 (线程之于程序)
* 生命周期:  `定义阶段`（Bean元信息配置）=> `实例化阶段`（创建Bean对象）=>`初始化阶段`（执行初始化逻辑）=>`使用阶段`
  （Bean可用）=>`销毁阶段`（释放资源）
* 目的在于降低使用门槛, 我不需要考虑执行的背后细节
* 使用方式:(  对XML文件的依赖程度逐渐降低 )
  * xml装配
  * 基于注解的自动扫描 (Spring最常见的方式)
  * java显式配置
* Spring中的注入方式:
  * 使用`@Autowired`注解 指定注入到其他的Bean中
  * 使用`@Resource`和`@Inject`注解
  * 使用XML注入
  * 构造方法自动注入

* 注意这里的版本变动 !!!
* In 1.0.0 these interfaces have been replaced:
  * CallAroundAdvisor → CallAdvisor,
  * StreamAroundAdvisor → StreamAdvisor,
  * CallAroundAdvisorChain → CallAdvisorChain
  * StreamAroundAdvisorChain → StreamAdvisorChain.
  * AdvisedRequest → ChatClientRequest
  * AdivsedResponse → ChatClientResponse.

---

## 第五次开发

2026年3月18日09:46:00

**RAG学习**

RAG（`Retrieval-Augmented Generation，检索增强生成`）是一种结合信息检索技术和 Al 内容生成的混合架构,可以解决大模型的知识时效性限制和幻觉问题。

有详细参考文献的AI 有根据

* RAG工作流程
  * 文档收集和切割
    * 收集并预处理
    * 清洗数据
    * 文档切割: 按大小切割 按语义界限切割 递归分割(AI分割)
  * 向量转换和存储
    * 向量转换Embeding模型转换, 不同向量表示的语义不同
  * 文档过滤和检索
    * 文档过滤和检索: 向量相似度检索 Rank模型排序 上下文组装
  * 查询增强和关联
    * 提示词组装 将相关文档和用户问题组合增强提示
    * 上下文融合: 大模型根据增强提示生成
    * 添加引用源
    * 后处理: 格式化 摘要 其他处理优化输出
* RAG的相关技术细则
  * Embeding模型: 将高维的离散数据 转化为低维的持续性向量
  * 维度越高语义精细度越高
  * 向量数据库: 专门存储和检索向量数据库
  * 召回: 在大量数据中 快速筛选相关的子集, 强调广度而非精度 => 粗排 => 精排

具体代码思路:

RAG

* 新建Rag包

* LawAPPDocumentLoader.java

  * 生成类
  * 创建构造器
  * 返回`List<document>`
  * 使用resourcePatternResolver获取路径下的md文件
  * 自定义文件加载器 (具体看官方文档) 自定义加载想要的配置
    * `for循环`加载MD文件配置, 将配置与`MarkdownDocumentReader`配合
    * 添加所有`MarkdownDocumentReader.get()`到`List`中返回
    * `try { ... } catch{ ... }`错误处理
  * 将源文件拆解为原信息( 所以要将文档尽量格式化 )
  * 添加测试类

* 向量数据库存储方式

  * 配置初始化 基于向量数据库的Bean
  * 使用alibaba的`Embedingmodel `(引入Spring ai的)出现Bean即为创建成功
  * 引入`SimpleVectorStore`
  * 将之前的`List<document>` 添加(add)

  *

* 查询增强( Advisor类还需要加强学习 )
  * `QuestionAnswerAdvisor`
  * `RetrievalAugmentationAdvisor`
* 在LawApp中新建VecterStore
  * 复用doChat
  * 在ChatClinet中开启``.advisor(new QuestionAnswerAdvisor(lawAppVectorStore))``
* 使用云知识库服务 + Spring AI
  * 使用国内的百炼平台 (为什么产业链先于AI开发? )
  * 开启会话改写, 将问题描述更加仔细
  * 上传云知识库
  * 打上更多的标签, 表格化存储, 更加细致化返回文档内容(`metadata`)
  * 最大分段长度: 每篇文章的最大长度
  * 分段重叠长度: 上下文重叠部分, 回答更加连贯
  * 复用doChat
  *

----

## 第六次开发

时间:2026年3月19日14:42:11

* MyLoggerAdvisor详解:
  * Logger实例
    * 在MyLoggerAdvisor类中定义一个静态的、不可变的日志记录器,用于输出该类的日志信息
    * 通过 SLF4J 的工厂方法，可以方便地在整个类中使用logger记录日志
  * .next方法
    * chain.nextStream(request)
    * chain.nextCall(request)

  RAG核心概念:


* **文档的收集和切割ETL**

  * 抽取 转换 加载 ( Extract Transform Load )
  * Spring ai中的文档是一个对象(document)
  * 核心组件: `DocumentReader DocumentTransformer DocumentWriter`
  * document不止可以读文档还有多媒体
  * **Extract 抽取**
    * 自主实现DocumentReader组件 先看官方的原码

  * `Transform 转化 - DocumentTransform`
    * `TextSplitter `分割文本
    * 实现类: `TokenTextSplitter`
    * 原信息增强器 `MetedataEnricher`: 补充更多的信息不改变切分规则( 关键字 总结 )
    * `contentFormatter` 格式化类

  * **Load 加载**
    * `DocumentWriter `基类  `FileDocumentWriter` 实现类

  * 整体流程:

* **向量的转换和存储**

  * 向量存储核心组件
  * `VectorStore` 核心交互接口
  * 搜索请求构造 `SearchRequest`
  * 过滤表达式: `filterExpression` 要用时专门看文档
  * 向量存储的工作原理
    1. 嵌入转换
    2. 相似度转换
    3. 相似度度量 :余弦相似度(夹角余弦值) 欧式距离 点积

* **PGVector**

  * 使用阿里云PGSQL
  * 创建高权限账号
  * 安装向量插件
  * 在idea中使用
  * 添加依赖

* 文档过滤和检索

  * 原始查询 => 优化查询
  * 检索多个文档
  * 查询转换
    * 改变询问方式
    * 改变查询语言
    * 查询压缩

----

## 第七次开发

时间2026年3月20日16:52:40

**RAG进阶**

* 优化原始文档 建议最好是Markdown文档
* 文档切片 切片大小和检索效果
* 结合**AI分块算法 和 人工二次校验** 效果更好
* 切词器使用python代码的语义切割 jieba库
* 元信息标注 手动添加原信息状态
* 向量转化存储
  * 综合比较开发成本

* 文档过滤和检索
  * 多查询扩展: 将问题拓展 更加准确 降低召回率
  * 低成本大模型执行问题拓展
  * 查询重写和翻译
  * 历史上下文改写
* 检索器配置
  * 设置合理的相似度
  * 控制返回文档的数量 召回片段数
  * 自定义检索增强工厂模式

具体代码实现:

* MyKeyWordEnricher
  * 大模型注入
  * 选择多少个关键词
  * 加载向量数据库
* 自定义检索增强工厂模式

----

## 第八次开发

时间: 2026年3月23日09:01:25

长沙出差回来
争取完成代码开发和理解

* 完成代码
  * com.lx.aisuperagent.rag.LawAppDocumentLoader代码完成

  * `Document`对象RAG信息的最小单位

    * 所有的外部数据都要转化为Document
    * 包含 `id content metadata `
    * `Builder()`模式配置
    * 支持多媒体( 文字 图片 ...)
    * `getFormattedContent` 获取格式化内容, 可选定 `MetadataMode:ALL, EMBED, INFERENCE, NONE`

  *  `MarkdownDocumentReader`

    * 调用底层 Markdown 引擎（Flexmark），将纯文本转为由` Header, Paragraph, List, Code `等组成的 **节点树 (Node Tree)**

    * 
  
  * `MarkdownDocumentReaderConfig`
  
    * 对文档获取的配置文件
  
    | **配置项**                     | **说明**                                                     |
          | ------------------------------ | :----------------------------------------------------------- |
    | `horizontalRuleCreateDocument` | 遇到 `---` 分隔线时，是否将其前后的内容切分为两个独立的 `Document`。 |
    | `includeCodeBlock`             | 是否保留代码块。在代码问答场景中必须为 `true`。              |
    | `includeBlockquote`            | 是否保留 `>` 引用块。                                        |
    | `additionalMetadata`           | 允许你手动注入全局元数据（如 `userId`, `category`）。        |



```plaintext
ChatClientResponse (外壳：ChatClient 专用)
└── .content() -> 直接获取字符串
└── .entity()  -> 自动转为 LawReport
└── .chatResponse() --------------------┐
↓
ChatResponse (核心：模型通用响应)
├── .getResults() -> 包含 AI 生成的文本
└── .getMetadata() -> 包含 Usage (Token 消耗)
```


* RAG本地知识库
  * 在resource中存放Markdown文档
  * LawAppDocumentLoader代码测试
  * 运行测试
  * 使用向量数据库对文档的检索
  * 优化输出时一并输出Token值
  * 

* RAG理论
  * 混合检索策略
  * 向量检索能理解语义, 但是不够精确
  * ElasticSearch - 精确检索, 但是不理解语义
  * 结构化检索
  * 级联混合检索 - - 层层筛选
* RAG开发评估: 如何确保真的有效
  * 检索质量
  * 生成回答质量: 事实准确性 ; 答案完整性 ; 上下文相关性; 引用准确性
* RAG评估流程
* 高级RAG架构
  * 自纠错RAG(C-RAG)
  * 自省RAG(Self-RAG) 我真的是否需要查询, 我是否需要调用 ==> 节省Token
  * 检索树RAG 对问题拆分  分别整合
  * 多智能体RAG 组合多个AI 通过通信协议
* 大模型幻觉
  * 自信满满地胡说八道



* 工具调用`Tool Calling`
  * 需求分析: 联网搜索; 网页抓取; 资源下载; 终端操作; 文件操作; PDF生成
  * 工具调用(Tool Calling) 借助外部工具调用 
  * 不是AI主动使用工具 而是工具返回结果给AI
  * `Function Calling == Tool Calling  : true`
* 流程:  
  * 

























































